using System.Windows;

namespace WpfApp
{
    /// <summary>
    /// Interaction logic for MainWindow.xaml
    /// This is the main window of our WPF application.
    /// It currently contains no UI elements, as per the initial request.
    /// </summary>
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            // Initializes the UI components defined in XAML
            InitializeComponent();
        }
    }
}
